# M1 model decision

Extraction, consolidation, and rerank stay on the GPT-5.6 family. Chat remains GPT-5.6 Luna at reasoning effort `high`.

The eval set is `docs/eval/m1-set.json` (24 hand-labeled documents covering all six memory kinds, 12 consolidate cases, 8 rerank cases). The harness is `packages/memory/src/eval.ts` and `packages/memory/src/run-m1-eval.ts` (`bun run eval:m1`).

Live candidate runs prefer **OpenRouter** (`openai/gpt-5.6-luna`, `openai/gpt-5.6-terra`) and fall back to the OpenAI API. The keyed run at `2026-09-08T02:37:32Z` used OpenRouter for every request. Raw per-example tokens, cost, and scores: `docs/eval/m1-results.json`.

## Extract (24 documents)

| Candidate | Precision | Recall | F1 | Input | Output | Reasoning | Cost | Latency |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Luna `none` | 0.535 | 0.708 | 0.593 | 5257 | 1737 | 0 | $0.0031 | 30.3s |
| Luna `low` | 0.542 | 0.708 | 0.599 | 5257 | 1786 | 36 | $0.0032 | 41.5s |
| Luna `medium` | 0.576 | 0.750 | 0.635 | 5257 | 2026 | 269 | $0.0035 | 36.5s |
| **Luna `high`** | **0.618** | **0.771** | **0.669** | 5257 | 2643 | 860 | $0.0042 | 50.0s |
| Terra `low` | 0.556 | 0.708 | 0.607 | 5257 | 1821 | 65 | $0.0324 | 40.0s |

Winner: **Luna `high`**. F1 is +0.034 over Luna `medium` and +0.076 over Luna `none`. Terra is ~10× the cost with worse quality. Luna `high` still costs $0.00017/doc on this set; revisit at Gmail volume (M5) if reasoning tokens blow up.

## Consolidate (12 cases)

Every candidate scored **accuracy 1.000**. Same-model-as-extract (Luna) therefore picks the cheapest effort: **`none`** ($0.0007, 12.1s, 0 reasoning tokens). Higher efforts only spent more reasoning tokens.

## Rerank (8 cases)

Every candidate scored **nDCG 1.000 / top-1 1.000**. **Luna `none`** wins on cost ($0.0004) and latency (6.9s vs 14.4s for Luna `low`).

## Pins

| Job | Model | Effort | Why |
|---|---|---|---|
| chat | `gpt-5.6-luna` | `high` | already locked |
| extract | `gpt-5.6-luna` | `high` | best F1; still the cheap 5.6 tier |
| consolidate | `gpt-5.6-luna` | `none` | perfect accuracy; same model as extract for prompt cache |
| rerank | `gpt-5.6-luna` | `none` | perfect ranking on the latency path |

Config: `defaultLlmConfig` in `packages/domain/src/llm.ts`. Re-run `bun run eval:m1` with `OPENROUTER_API_KEY` (or `OPENAI_API_KEY` as fallback) before changing these pins.
